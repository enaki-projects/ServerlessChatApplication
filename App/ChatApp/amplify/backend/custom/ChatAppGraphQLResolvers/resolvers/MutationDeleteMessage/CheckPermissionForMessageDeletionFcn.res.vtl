#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#if($ctx.result.authorId != $ctx.identity.claims.get("sub"))
    $util.error("Not authorized to perform deletion for this message")
#end

$util.toJson({})
