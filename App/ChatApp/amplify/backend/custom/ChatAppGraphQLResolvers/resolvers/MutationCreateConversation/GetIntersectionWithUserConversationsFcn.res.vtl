#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#foreach( $item in $ctx.result.items )
	$util.qr($ctx.stash.userConversationsIds.add($item.conversationId))
#end

## Pass back the result from DynamoDB. **
$util.toJson({})
