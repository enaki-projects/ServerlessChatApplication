#if($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end

#set( $items = ${ctx.result.data.get($ctx.stash.DS.ConversationTable)} )

#foreach( $item in $items )
	#if( $item.isGroup == false )
    	$util.qr($ctx.stash.put("conversationExists", true))
    	#return
    #end
#end

{}
